require 'spec_helper'

describe Shrine::Encoder do
  it 'has a version number' do
    expect(Shrine::Encoder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
